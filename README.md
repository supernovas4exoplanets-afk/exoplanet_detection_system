# exoplanet_detection_system

### Extract_light_curves.py
This script processes FITS files to extract light curves, normalizes the flux, removes NaNs, and performs a Box Least Squares (BLS) search to identify periodic signals. It saves the flattened light curve, BLS periodogram, and phase folded light curve for each TIC ID found in the filenames.
